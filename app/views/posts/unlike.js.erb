$("#like_<%= @post.id %>").removeClass('glyphicon-heart').addClass('glyphicon-heart-empty').attr("href", "<%= @post.id %>/like");

$("#likes_<%= @post.id %>").html("<%= j (render partial: 'posts/likes', locals: { post: @post} ) %>");
